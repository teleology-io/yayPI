version: "1"
kind: auth

auth:
  base_path: /auth
  user_entity: User

  register:
    enabled: true
    credential_field: email
    password_field: password
    hash_field: password_hash
    default_role: viewer

  login:
    enabled: true
    credential_field: email
    password_field: password
    hash_field: password_hash

  me:
    enabled: true

  # oauth2:
  #   providers:
  #     - name: google
  #       client_id: ${GOOGLE_CLIENT_ID}
  #       client_secret: ${GOOGLE_CLIENT_SECRET}
  #       redirect_uri: ${APP_URL:-http://localhost:8080}/api/v1/auth/callback/google
  #       success_redirect: ${FRONTEND_URL:-http://localhost:3000}
  #       error_redirect: ${FRONTEND_URL:-http://localhost:3000}/login
  #
  #     - name: github
  #       client_id: ${GITHUB_CLIENT_ID}
  #       client_secret: ${GITHUB_CLIENT_SECRET}
  #       redirect_uri: ${APP_URL:-http://localhost:8080}/api/v1/auth/callback/github
  #       success_redirect: ${FRONTEND_URL:-http://localhost:3000}
  #       error_redirect: ${FRONTEND_URL:-http://localhost:3000}/login
